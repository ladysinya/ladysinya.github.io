class CharacterDetails {
    async init() {
        console.log('init');
        var convert = require('xml-js');
        // const xmlData = await readAsync('LVL2.xml', 'utf8');
        
        // const parseString = require('xml2js').parseString;
        // const result = await parseString(xmlData);
        // console.log(result);
    }
}